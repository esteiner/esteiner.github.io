# Credentials for the pre-seeded test account. The e2e suite starts its own Pod
# on http://localhost:3001 (WebID: http://localhost:3001/edwin/profile/card#me);
# you do NOT need to run a Pod yourself.
# Copy this file to e2e/.env and fill in the real values. e2e/.env is gitignored.
E2E_EMAIL=you@example.com
E2E_PASSWORD=change-me

# Optional: point the tests at an already-running app instead of the one
# Playwright starts. Defaults to http://localhost:5173 (Vite dev server).
# E2E_BASE_URL=http://localhost:5173

# Optional: override the self-started Pod's origin/port (default 3001).
# E2E_POD_ORIGIN=http://localhost:3001
